#include <stdio.h>
#include <string.h>

/**
 * main - code entry poinit
 *
 * Return: 0 hopefully
 */

int main(void)
{
	char phrase[52];

	strcpy(phrase, "\"Programming is like building a multilingual puzzle");

	puts(phrase);

	return (0);

}

#include "main.h"

/**
 * mul - returns multiplication of two integer
 * @a: first term
 * @b: second term
 * Return: the multiplication
 */

int mul(int a, int b)
{
	int result = a * b;

	return (result);
}

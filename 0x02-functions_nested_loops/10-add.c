
/**
 * add - adds two numbers
 * @n0: first term
 * @n2: second term
 *
 * Return: the result.
 * On error, -1 is returned, and errno is set appropriately.
 */
int add(int n0, n1)
{
	return (n0 + n0);
}
